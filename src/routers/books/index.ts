import { Router } from "express";

const bookRouter = Router();

bookRouter.post('', );
bookRouter.get('', );
bookRouter.get('/:id', );
bookRouter.put('/:id', );
bookRouter.delete('/:id', );

export default bookRouter;